{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cp .env.example .env && composer install && php artisan key:generate"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan migrate:fresh --force && php artisan serve --host=0.0.0.0 --port=8080"
  }
}
