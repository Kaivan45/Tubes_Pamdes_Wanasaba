{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install && php artisan config:clear && php artisan cache:clear && php artisan key:generate"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force || true && php artisan serve --host=0.0.0.0 --port=8080"
  }
}
